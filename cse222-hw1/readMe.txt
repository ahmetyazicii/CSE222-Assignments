Java codes(.java) are in src folder. There is a Makefile in that folder. 
To run Makefile it is enough to write "make" on terminal.
Also savefile.txt (the save file of program) is in src folder.
I used System.getProperty("user.dir"); then added it to "/savefile.txt" string.
This is file path.
